# --- Artefatti e asset SOLO-LOCALI (mai nel repo pubblico) ---

# mockup e riferimenti di design interni
/design/

# proxy server-side (Google Apps Script), non parte del sito statico
/apps_script.gs

# QR del solo URL pubblico, tenuto in locale
/qr.png

# collegamenti alle note del Second Brain (privato)
/PROJECT_NOTES.md

# screenshot/snapshot generati durante i test
/.playwright-mcp/

# harness E2E locale (camera finta Chromium + frame generati)
/test_e2e/

# QR del check-in (asset locale)
/qr_checkin.png
/qr_checkin.svg

# Locandina generata (asset locale)
/locandina_checkin.png

# Locandine con QR WiFi (password) — MAI nel repo pubblico
/locandina_3qr_checkin.png
/locandina_3qr_checkin.pdf
/OlasDelSur_locandina_ospiti*.pdf
